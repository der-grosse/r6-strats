import { Metadata } from "next";

export const metadata: Metadata = {
  title: "R6 Strats Login",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
